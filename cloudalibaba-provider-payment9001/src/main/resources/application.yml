server:
  port: 9001
# 注册 注册中心
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
# 暴露信息用于监控
management:
  endpoints:
    web:
      exposure:
        include: '*'
#